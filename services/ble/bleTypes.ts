// BLE primitive types and connection strength utility type.
// Intentionally no references to device types to avoid circular deps.

export type ConnectionStrength = 'strong' | 'medium' | 'weak' | 'disconnected';

